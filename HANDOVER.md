# Modeling Harness 交接说明

## 1. 项目定位

`modeling-harness` 是一个面向通用数学建模研究的隔离式多智能体运行框架，
Agent 身份为 `generic-agent-v2.5`，包版本为 `2.5.0`。它的目标不是为某一道
题、某个竞赛或既有答案定制求解器，而是提供一条可审查的通用工作流：面对一个
此前未见的现实问题、原始附件与有限资源，组织多个职责隔离的角色形成完整研究
交付。

系统的目标交付不止是“给出一个模型想法”。每项任务应最终形成结构化报告、可
运行代码与依赖说明、数据与证据的溯源关系、哈希化工件清单、独立复算记录，以及
符合当前任务交付协议的文件包。竞赛或机构的格式要求只作为任务级
`Submission Profile` 约束输出接口，不能修改 Agent 本体、角色路由或通用能力。

本仓库公开的是 Agent 运行包：实现、角色配置、提示词、模式、文档和容器定义。
评测题目、原始附件、留出数据、运行输出、评审材料与私有诊断工件均不属于公开
发布范围，并由 `.gitignore` 明确排除。

## 2. 核心思路：定义—理解—创造—验证—交付

系统将建模任务分成五个彼此可追踪的阶段，而非由单一角色直接生成最终答案：

1. **定义（Define）**：解析任务、子问、约束、单位、附件和交付要求；将自然
   语言要求转成可检查的任务包、范围和验收标准。
2. **理解（Understand）**：建立数据字典、来源与证据链；识别缺失、异常、泄漏、
   假设边界、机制解释与不可识别性。
3. **创造（Create）**：比较基线与候选模型，选择机制模型、统计学习、优化决策或
   数值计算路径；要求模型可解释、可行并说明适用前提。
4. **验证（Verify）**：检查推导、单位、约束、代码复现、独立复算、敏感性、随机
   稳定性和跨工件一致性；证据不足时应报告边界，而不是编造确定结论。
5. **交付（Deliver）**：整合报告、代码、数据说明、图表、复现入口、工件哈希和
   任务级格式检查，输出可审查的提交包。

这五阶段是能力与工件的闭环，不是允许跳过审查的线性文案模板。后续角色只能接收
经主控批准、带内容哈希的工件；未提升的工作区内容不能作为正式输入。

## 3. 角色与责任边界

系统注册 16 个角色，其职责可归纳如下：

| 职能层 | 代表角色 | 责任 |
| --- | --- | --- |
| 编排与隔离 | `system_architect`、`sandbox_platform_engineer` | 定义运行契约、隔离边界、资源限制、故障恢复与接口。 |
| 问题与证据 | `problem_definition_router`、`domain_evidence_researcher`、`data_assumption_analyst` | 覆盖子问与约束，建立来源、数据和假设边界。 |
| 建模创造 | `model_architect`、`mechanistic_numerical_modeler`、`statistical_learning_modeler`、`optimization_decision_modeler` | 构造、比较并求解适配的模型与算法。 |
| 计算与表达 | `compute_reproducibility_engineer`、`visualization_report_author` | 固化依赖和运行入口，生成可复现结果与清晰报告。 |
| 独立审查 | `mathematical_reviewer`、`reproducibility_reviewer`、`evidence_communication_reviewer`、`standards_delivery_manager` | 检查数学逻辑、独立复算、证据与表达一致性、交付协议。 |
| 基准治理 | `benchmark_curator` | 管理隔离基准与评测协议，但不将题目内容带入 Agent 本体修改。 |

主控 Agent 只负责编排、授权、转译审查结果、批准工件提升与汇总状态；它不应直接
替代角色完成建模生产工作。角色定义位于 `.codex/agents/`，注册表与状态机位于
`workspaces/architect/`，各角色系统提示词位于
`workspaces/prompts/system_prompts/`。

## 4. 工件、隔离与可追溯性设计

运行时由 `Orchestrator` 生成角色调度记录和 `SandboxRequest`。每次调度应当具备：

- 独立可写工作区、会话与尝试标识；
- 只读的已提升输入，且每项输入携带内容哈希与来源信息；
- 明确的 CPU、内存、磁盘、时限和网络策略；
- 对其他 Agent 工作区的禁止访问；
- 由角色、提示词与工件绑定产生的任务包；
- 输出后进行模式、绑定关系和审查门槛检查。

正式提升遵循“作者工作区与评审环境分离”的原则：评审环境重新创建，不继承作者的
运行状态；对外部工件的使用通过清单与哈希记录。设计上，这使报告中的结论能够
回溯到数据、假设、代码、推导和来源，而不是仅依赖语言模型的自然语言叙述。

关键实现位于：

- `src/modeling_harness/orchestrator.py`：调度、阶段推进、工件输入与执行请求组装；
- `src/modeling_harness/isolation.py`、`runtime.py`：沙箱计划、运行时和限制；
- `src/modeling_harness/artifacts.py`、`packets.py`、`schemas.py`：工件、数据包与模式；
- `src/modeling_harness/governance.py`、`evaluation.py`：治理、评测与门槛逻辑；
- `workspaces/architect/schemas/`：任务包、结果包、审查包和授权记录等 JSON Schema。

## 5. 反特化与评测治理

本项目最重要的治理约束是：历史真题、变体题、当前任务、参考答案、评分、失败
位置、评审意见以及它们的派生信息，都只可用于当前答案工件修订或隔离评估；它们
不能成为修改 Agent 本体的动机、证据或触发器。

允许修改 Agent 本体的来源仅限于：

- 用户直接给出的通用治理要求；或
- 与具体任务无关、可独立审计的一般研究。

任何候选修改都必须以跨领域、未见任务与回归验证检验其通用性，且不能包含题号、
题名、领域标签、特定参数、答案、失败位置或针对某题的工具策略。内部评测分数
只能用于比较版本的可靠性、完整性、可复现性和跨任务稳定性，不能解释为官方名次
或获奖概率。

这条边界也意味着：即使某次评测揭示缺陷，构建侧接收的也应是抽象能力缺陷，例如
“长期约束追踪不足”，而不是具体题目、数据或答案。测试工件必须与公开源码和
Agent 本体的演进链保持隔离。

## 6. 运行入口与近期 P0 修复

常用源码校验命令如下：

```console
modeling-harness validate-config --project-root .
modeling-harness validate-codex-adapter --project-root .
modeling-harness production-preflight --json
modeling-harness doctor --project-root .
```

也可使用 `python -m modeling_harness.cli <command>`。在开始任何实际隔离运行前，
应先安装项目声明的依赖，并配置一个经验证的隔离容器后端和镜像。

近期已闭合一个确定性的 P0 源码级问题：调度器此前向容器追加
`modeling-harness-agent <role_id>`，但镜像中的 Python worker 要求
`--role <role> --charter <path>`。当前 `Orchestrator` 已统一调用
`codex_agent_entrypoint()`，由角色注册表生成规范参数；回归测试明确断言该调度
请求使用 worker 所需的 `--role` 与 `--charter` 参数。这是任务无关的运行契约修复，
不是针对某道题的优化。

容器镜像入口定义在 `containers/codex-agent/Dockerfile`，worker 参数解析与执行逻辑
位于 `containers/codex-agent/codex_worker.py`，适配器位于
`src/modeling_harness/codex_adapter.py`。

## 7. 当前证据状态与限制

当前源码声明的冻结候选为
`generic-agent-v2.5-source-freeze-s002`，状态是
`SOURCE_FROZEN_DIAGNOSTIC_CANDIDATE`。该名称表示源码与配置已接受诊断级检查，
不表示生产发布、数学结论正确性或端到端交付能力已经成立。

目前可确认的证据包括：

- 配置、角色和适配器的源码级检查可运行；
- P0 启动参数契约已有专门回归断言；
- 最近一次本地全量测试结果为 **302 通过、3 跳过、0 失败**；跳过项均为 Windows
  环境中无法创建符号链接的能力限制。

仍然**不能**据此声称系统生产就绪，原因包括：

- Docker/隔离后端的真实角色启动尚未作为当前候选的运行证据建立；
- 尚无运行时封印、不可变镜像绑定或依赖锁定的正式证明；
- 尚未建立一次完整的“任务输入 → 非空可追溯工件 → 独立复算 → 报告与交付包”
  运行证据；
- 结果包的字段模式检查不能自动等价于跨工件的科学语义真实性；
- 正式发布资格、生产资格与形式化泛化资格仍为 `NO` / `CLOSED`。

因此，P0 修复只解决了“角色能以正确参数被请求启动”的必要条件；它没有证明容器
能够成功执行，更没有证明系统可无监督完成数学建模或满足任何竞赛准入条件。

## 8. 下一位维护者的推荐工作顺序

以下工作应按证据链推进，并保持与评测题目内容隔离：

1. 在干净的、受控的容器环境中，针对每个角色执行最小启动烟雾测试，保存镜像
   摘要、依赖锁定、命令行、退出状态和工件哈希。
2. 建立可操作的端到端任务入口：接收任务包、调度角色、汇总经批准工件并产生
   交付包；该入口应只接收任务级输入，不能改变 Agent 本体。
3. 加强结果语义验证：对主张—证据—假设—代码—结果之间的引用完整性、非空性、
   单位、约束和独立复算进行跨工件检查，而不只依赖 JSON Schema。
4. 在隔离评测平面执行端到端验证。评测结论只能决定该候选是否通过或失败，不能
   反向驱动 Agent 本体修改；若需改进，必须走直接用户治理或独立一般研究授权。
5. 只有在隔离、复现、交付格式与独立验证均具备确凿记录后，才可重新评估运行时
   或生产资格。

## 9. 版本控制与发布说明

公开远端为：<https://github.com/GavinNull/modeling-harness>。

每次公开发布前应只暂存明确属于源码运行包的文件，避免使用不加区分的
`git add -A`。尤其不得提交 `benchmarks/`、`pilot_runs/`、`tests/`、`output/`、
`research/`、本地依赖目录或临时工作区；当前 `.gitignore` 已将这些范围列为公开
发布边界。发布后至少核对远端默认分支、提交哈希与关键文件内容。

本说明的用途是帮助维护者正确接手系统，而不是授权绕过隔离、将评测信息用于
特化，或将现有源码级测试结果表述为真实数学建模能力证明。
