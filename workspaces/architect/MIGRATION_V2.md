# Historical migration notice

This document previously described a pre-2.5 admission topology. That topology
is retired and has no executable or documentary authority in package 2.5.0.

The only current Agent-body construction lineages are the closed DirectUser and
independent-general-research lineages declared by `sound_boundary_r3e` in the
canonical registry, state machine, architecture, prompts, profiles, schemas,
and runtime. Evaluation or release objects are never construction parents.

Use `MIGRATION_V2_5.md` for the current migration contract.
